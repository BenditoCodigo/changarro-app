{
  "name": "changarro-electron",
  "private": true,
  "version": "0.0.0",
  "description": "Electron platform project for Changarro.",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc",
    "start": "electron .",
    "pack": "npm run build && npx capacitor-electron vendor && npx electron-builder --config electron-builder.config.js"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.5.0"
  }
}
